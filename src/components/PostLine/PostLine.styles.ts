import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  labelText: {
    fontWeight: 600,
    marginTop: 8,
  },

  valueLineText: {
    marginVertical: 4,
    fontWeight: 400,
  },
});
