import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    marginVertical: 4,
    padding: 4,
    borderColor: '#ddd',
    borderWidth: 1,
    borderRadius: 4,
    backgroundColor: '#eee',
  },
  sectionTitle: {
    fontSize: 16,
    fontWeight: 700,
    textTransform: 'uppercase',
  },
});
