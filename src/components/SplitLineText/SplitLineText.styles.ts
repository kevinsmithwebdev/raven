import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  labelText: {
    fontWeight: 600,
    fontStyle: 'italic',
    color: '#222',
  },
  lineText: {
    marginVertical: 4,
    fontWeight: 400,
    color: '#222',
  },
});
