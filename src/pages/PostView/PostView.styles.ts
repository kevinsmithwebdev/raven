import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    padding: 8,
  },
  section: {
    marginVertical: 4,
    padding: 4,
    borderColor: '#ccc',
    borderWidth: 1,
    borderRadius: 4,
    backgroundColor: '#eee',
  },
  sectionTitle: {
    fontSize: 16,
    fontWeight: 700,
  },
});
