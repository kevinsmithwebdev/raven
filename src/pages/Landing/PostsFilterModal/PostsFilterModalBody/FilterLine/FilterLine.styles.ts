import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  filterLine: {
    paddingHorizontal: 8,
    paddingVertical: 12,
    borderColor: '#aaa',
    borderBottomWidth: 1,
    backgroundColor: '#eee',
  },
  filterLineSelected: {
    backgroundColor: '#0047AB',
  },
  filterLineText: {
    color: '#0047AB',
  },
  filterLineTextSelected: {
    color: '#fff',
  },
});
